import { ChatGate } from "@/components/chat/chat-gate";

// Nav-panel route: /drafts. The active panel is read from the URL by ChatProvider.
export default function DraftsPage() {
  return <ChatGate />;
}
