import { ChatGate } from "@/components/chat/chat-gate";

// Nav-panel route: /mentions. The active panel is read from the URL by ChatProvider.
export default function MentionsPage() {
  return <ChatGate />;
}
