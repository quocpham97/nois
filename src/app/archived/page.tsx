import { ChatGate } from "@/components/chat/chat-gate";

// Nav-panel route: /archived. The active panel is read from the URL by ChatProvider.
export default function ArchivedPage() {
  return <ChatGate />;
}
