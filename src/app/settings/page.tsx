import { ChatGate } from "@/components/chat/chat-gate";

// Settings route: /settings. ChatProvider reads the panel/overlay from the URL.
export default function SettingsPage() {
  return <ChatGate />;
}
