import { ChatGate } from "@/components/chat/chat-gate";

// Nav-panel route: /people. The active panel is read from the URL by ChatProvider.
export default function PeoplePage() {
  return <ChatGate />;
}
